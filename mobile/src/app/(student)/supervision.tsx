import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function StudentSupervision() {
  return <ScreenPlaceholder title="Supervision" note="Built in Phase 5 (Supervisor Workflows)." />;
}
