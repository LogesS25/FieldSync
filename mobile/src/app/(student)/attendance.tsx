import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function StudentAttendance() {
  return <ScreenPlaceholder title="Attendance" note="Built in Phase 4 (Student Fieldwork)." />;
}
