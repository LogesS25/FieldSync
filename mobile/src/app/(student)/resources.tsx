import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function StudentResources() {
  return (
    <ScreenPlaceholder icon="book-outline" title="Resource Library" note="Built in Phase 7 (Resources & Notifications)." />
  );
}
