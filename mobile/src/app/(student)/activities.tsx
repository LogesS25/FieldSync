import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function StudentActivities() {
  return <ScreenPlaceholder title="Field Activities" note="Built in Phase 4 (Student Fieldwork)." />;
}
